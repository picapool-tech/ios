name: Test Dummy IPA Upload (Fixed Key)

on:
  workflow_dispatch:
  push:
    branches: [ under_dev ]

jobs:
  ios-upload:
    runs-on: macos-14

    env:
      P12_FILE_BASE64:        ${{ secrets.P12_FILE_BASE64 }}
      P12_PASSWORD:           ${{ secrets.P12_PASSWORD }}
      MOBILEPROVISION_BASE64: ${{ secrets.MOBILEPROVISION_BASE64 }}
      ISSUER_ID:              ${{ secrets.APP_STORE_ISSUER_ID }}
      KEY_ID:                 6N8VW75J28

    steps:
    - name: Create dummy IPA
      run: |
        mkdir -p build/ios/ipa
        echo "dummy" > build/ios/ipa/dummy.txt
        zip -r build/ios/ipa/dummy.ipa build/ios/ipa/dummy.txt

    - name: Import signing certificate
      uses: apple-actions/import-codesign-certs@v2
      with:
        p12-file-base64: ${{ env.P12_FILE_BASE64 }}
        p12-password:    ${{ env.P12_PASSWORD }}

    - name: Install provisioning profile
      run: |
        echo "$MOBILEPROVISION_BASE64" | base64 --decode > ~/profile.mobileprovision
        mkdir -p ~/Library/MobileDevice/Provisioning\ Profiles
        cp ~/profile.mobileprovision ~/Library/MobileDevice/Provisioning\ Profiles/

    - name: Write API Key with Proper Newlines
      run: |
        mkdir -p ~/private_keys
        printf "%s\n" \
        "-----BEGIN PRIVATE KEY-----" \
        "MIGTAgEAMBMGByqGSM49AgEGCCqGSM49AwEHBHkwdwIBAQQg/e7TcBqDeQhSbgYv" \
        "XbwWSGNXRR+IWZJyxJvSWaOCpc2gCgYIKoZIzj0DAQehRANCAARbsiFB3VpxRV45" \
        "9PTS6h5eg6G0N4UNxRKZGuIwNHZrU3gxAXcREKOWurWGvw6BVJ15ix2xC7pS452e" \
        "h7G8KDR0" \
        "-----END PRIVATE KEY-----" \
        > ~/private_keys/AuthKey_${KEY_ID}.p8

        echo "=== Debugging Key ==="
        cat ~/private_keys/AuthKey_${KEY_ID}.p8
        echo "===================="

    - name: Upload to TestFlight
      uses: apple-actions/upload-testflight-build@v1
      with:
        app-path: build/ios/ipa/dummy.ipa
        issuer-id:       ${{ env.ISSUER_ID }}
        api-key-id:      ${{ env.KEY_ID }}
        api-private-key: ~/private_keys/AuthKey_${{ env.KEY_ID }}.p8
